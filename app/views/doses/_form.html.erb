<%= simple_form_for([cocktail, dose]) do |f| %>
  <%= f.association :ingredient, collection: Ingredient.order(:name) %>
  <%= f.input :description, as: :text, input_html: { placeholder: "20ml" } %>
  <%= f.button :submit, value: "Add an ingredient" %>
<% end %>
