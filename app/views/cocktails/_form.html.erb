<%= simple_form_for(cocktail) do |f| %>
  <%= f.input :name, input_html: { placeholder: "Enter your cocktail’s name…" } %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.button :submit %>
<% end %>
